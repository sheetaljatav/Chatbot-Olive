# Copy to `secret.yaml`, fill in values, then `kubectl apply -f secret.yaml`.
# DO NOT commit a populated version of this file.
apiVersion: v1
kind: Secret
metadata:
  name: chatbot-secrets
  namespace: chatbot
type: Opaque
stringData:
  GEMINI_API_KEY: ""
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
  POSTGRES_PASSWORD: "chatbot"
